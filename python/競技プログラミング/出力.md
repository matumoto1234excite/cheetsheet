# 出力

```python
print(a, b)           # 空白区切りでaとbを出力
print(a, b, sep="\n") # 改行区切りでaとbを出力
print(a, b, end="")     # 出力後の文字列を変更（デフォルトでは"\n"になっている）
print(1==1) # True Falseを出力（この場合はTrue）
print(int(1==1)) # True Falseを1 0で出力（この場合は1）
```
