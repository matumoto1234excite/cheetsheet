# 辞書

```python
dic = {} # 空の辞書(dictは予約語なので使えない)

dic["hoge"]=1 # 値の挿入(元々キーがないと操作は不可能)
dic["aiu"] += 1 # "aiu"がキーにない場合エラー

print(dic.values()) # 全ての値を出力
print(dic.keys()) # 全てのキーを出力
print(dic["aiu"]) # "aiu"キーの値を出力
```

